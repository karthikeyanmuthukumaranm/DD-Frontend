import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.karthikeyan.insurancemanager',
  appName: 'Insurance Manager',
  webDir: 'dist'
};

export default config;
